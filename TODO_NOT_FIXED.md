# 未完成与未修好

> 规则：这里只放“还没结案”的事情。做完了就移到优化日志，不要长期堆在这里。

## P0 - 当前必须继续

### 进度区整块闪动 / 跑马灯残留
- 状态：未结案
- 当前确认根因：
  - `Forms/OperationProgressDialog.cs` 仍在使用 `ProgressBarStyle.Marquee`
  - `Forms/OperationProgressDialog.cs` 在 `Apply()` 后直接 `Refresh()`
  - `Forms/MainForm.cs` 后台任务卡仍在使用 `ProgressBarStyle.Marquee`
- 当前进展：
  - 2026-04-24 已完成代码层移除 `Marquee`
  - 2026-04-24 已完成代码层去掉进度窗 `Refresh()`
  - 2026-04-24 已完成后台任务状态节流与去重
- 下一步：
  - 用安装版实机确认“只剩百分比跳动 + 静态绿色填充”
  - 确认未知进度时不再出现整块流动或闪白

### 主窗口缩放卡顿 / 变形 / 白屏体感
- 状态：未结案
- 当前确认根因：
  - 拖动缩放时仍会反复进入重型 `RefreshScaledUi()`
  - 任务卡宽度与图标列失效刷新在缩放过程中仍过于频繁
- 当前进展：
  - 2026-04-24 已把主窗拖动缩放改成“拖动时不做重型刷新，稳定后再批量刷新”
  - 2026-04-24 已把主窗图标列刷新与缩放过程解耦
  - 2026-04-24 已把 `C盘建议` 图标列刷新与缩放过程解耦
- 下一步：
  - 用安装版实机确认“小窗变大窗 / 大窗变小窗”体感是否明显变稳
  - 继续看是否还会出现顶部变形或数据区挤压

### 主窗口小窗数据区仍然太少
- 状态：未结案
- 用户最新反馈：2026-04-24 仍反馈“看到的数据还是太少”
- 现象：
  - 顶部仍占高过多
  - 真正数据区仍然偏小
- 下一步：
  - 继续压缩顶部多行结构
  - 必要时把多条工具行合并

### 主窗口顶部区仍然过高
- 状态：未结案
- 当前已尝试：
  - 运行信息隐藏
  - 任务卡片改摘要
  - 汇总第二行改悬浮
- 仍然不够的原因：
  - 视图切换、主按钮、教学条、盘符条、筛选条、汇总条合计仍然太高

## P1 - 重要但可随后处理

### 纯黑科技感主题仍未统一
- 状态：未结案
- 现象：
  - 主窗和多个对话框仍是浅色卡片风
  - 和已锁定的“纯黑简约科技感”方向不一致
- 当前进展：
  - 2026-04-24 已新增统一暗黑主题令牌 `Infrastructure/UiThemePalette.cs`
  - 2026-04-24 已把主窗、C盘建议、进度窗、设置窗、结案清单窗、确认窗、部署窗、提权提示窗接入暗黑主题代码
- 下一步：
  - 用安装版逐窗验收是否仍残留浅色控件 / 白底卡片
  - 继续补强菜单、状态细节和极个别高亮色一致性

### 图标 fallback 质量不达标
- 状态：未结案
- 现象：
  - 仍使用系统通用图标兜底
  - 小尺寸时容易糊，像普通白绿点框
- 当前进展：
  - 2026-04-24 已把 fallback 从 `SystemIcons.Application` 主路径升级为应用自身官方主图标
  - 2026-04-24 已增加尺寸感知和空白图标校验
- 下一步：
  - 用安装版真实列表确认是否还会看到“白绿点框感”
  - 继续回查少数抽取失败的软件项

### 启动速度仍未达到“秒开感”
- 状态：未结案
- 现象：
  - 首屏虽然已有分阶段加载，但首次可见与增量刷新仍有优化空间
- 当前进展：
  - 2026-04-24 已通过任务状态节流、去掉进度整窗刷新、减少缩放期重排与图标列乱刷，先压掉一批无效 UI 开销
- 下一步：
  - 继续减少首屏整窗刷新
  - 只对当前活动视图做最小化绑定和刷新

### 退出后进程残留
- 状态：未结案
- 说明：
  - 之前部署时发现窗口关闭后进程未完全退出，导致安装目录 EXE 被占用
- 最新发现：
  - 2026-04-24 再次发布安装版时，确实命中 `D:\\磁盘清理器\\磁盘清理器.exe` 进程占用
  - 本轮发布前已手动结束旧进程，随后部署成功
- 风险：
  - 会影响后续同步和用户体验

## P2 - 后续增强

### 让日志记录更自动化
- 状态：待做
- 方向：
  - 继续减少手写遗漏，把更多状态写回标准 checkpoint 流程

### 让检查点写回更细粒度
- 状态：待做
- 方向：
  - 未来可考虑把更多任务阶段自动拆成开始 / 子步骤 / 完成三级记录
