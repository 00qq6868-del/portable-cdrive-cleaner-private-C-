# 未完成与未修好

> 规则：这里只放“还没结案”的事情。做完了就移到优化日志，不要长期堆在这里。

## P0 - 当前必须继续

### 进度区整块闪动 / 跑马灯残留
- 状态：未结案
- 当前确认根因：
  - `Forms/OperationProgressDialog.cs` 仍在使用 `ProgressBarStyle.Marquee`
  - `Forms/OperationProgressDialog.cs` 在 `Apply()` 后直接 `Refresh()`
  - `Forms/MainForm.cs` 后台任务卡仍在使用 `ProgressBarStyle.Marquee`
- 下一步：
  - 全面移除 `Marquee`
  - 去掉整窗 `Refresh()`
  - 让百分比文字和静态绿色填充成为唯一动态反馈

### 主窗口缩放卡顿 / 变形 / 白屏体感
- 状态：未结案
- 当前确认根因：
  - 拖动缩放时仍会反复进入重型 `RefreshScaledUi()`
  - 任务卡宽度与图标列失效刷新在缩放过程中仍过于频繁
- 下一步：
  - 缩放过程只做轻量判断
  - 重型布局、图标列刷新延后到稳定后统一执行

### 主窗口小窗数据区仍然太少
- 状态：未结案
- 用户最新反馈：2026-04-24 仍反馈“看到的数据还是太少”
- 现象：
  - 顶部仍占高过多
  - 真正数据区仍然偏小
- 下一步：
  - 继续压缩顶部多行结构
  - 必要时把多条工具行合并

### 主窗口顶部区仍然过高
- 状态：未结案
- 当前已尝试：
  - 运行信息隐藏
  - 任务卡片改摘要
  - 汇总第二行改悬浮
- 仍然不够的原因：
  - 视图切换、主按钮、教学条、盘符条、筛选条、汇总条合计仍然太高

## P1 - 重要但可随后处理

### 纯黑科技感主题仍未统一
- 状态：未结案
- 现象：
  - 主窗和多个对话框仍是浅色卡片风
  - 和已锁定的“纯黑简约科技感”方向不一致
- 下一步：
  - 建立统一主题令牌
  - 先覆盖主窗、C盘建议、进度窗、设置窗、结案清单窗

### 图标 fallback 质量不达标
- 状态：未结案
- 现象：
  - 仍使用系统通用图标兜底
  - 小尺寸时容易糊，像普通白绿点框
- 下一步：
  - 改为应用图标优先 + 官方主图标兜底
  - 支持更合理的尺寸与缓存策略

### 启动速度仍未达到“秒开感”
- 状态：未结案
- 现象：
  - 首屏虽然已有分阶段加载，但首次可见与增量刷新仍有优化空间
- 下一步：
  - 继续减少首屏整窗刷新
  - 只对当前活动视图做最小化绑定和刷新

### 退出后进程残留
- 状态：未结案
- 说明：
  - 之前部署时发现窗口关闭后进程未完全退出，导致安装目录 EXE 被占用
- 风险：
  - 会影响后续同步和用户体验

## P2 - 后续增强

### 让日志记录更自动化
- 状态：待做
- 方向：
  - 继续减少手写遗漏，把更多状态写回标准 checkpoint 流程

### 让检查点写回更细粒度
- 状态：待做
- 方向：
  - 未来可考虑把更多任务阶段自动拆成开始 / 子步骤 / 完成三级记录
