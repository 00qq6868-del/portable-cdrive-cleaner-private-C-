# 持久记忆

## 用户硬性要求
- 不要遗忘历史要求，必须把记忆写进项目文件，而不是只靠聊天上下文
- 同一个 AI 的不同窗口、不同 AI 的不同窗口，都要能接着做
- 不同 AI、不同窗口，不只是继续对话，还要能继续修改、优化、回调、回查
- 所有数据都要实时保存到 GitHub 私有仓库，不能等全部做完才补写
- 保留所有重要数据、已完成进度、未完成进度、被打断进度，方便在别的窗口继续回退、修改、增加、优化
- 删除或合并冗余重复记录时，只能删重复噪音，不能误删关键历史、关键决策、关键未完成项
- 所有“做到哪一步、没做到哪一步、下一步做什么”，必须可追溯
- 每次开始新的优化任务前，要先把计划和当前状态写下来
- 每完成一个部分，就立刻记录一个部分
- 已完成、未完成、待验证、回归失败，都必须明确记录
- 如果对话可能被截断，优先保存：
  - 当前进度
  - 当前计划
  - 修改过的文件
  - 源码修改方案
  - 已经修改过的方案
  - 还没进行的方案
  - 正在进行但未完成的方案
  - 还没修好的地方
  - 下一步该做什么
- 文件夹里的安装版和桌面入口要始终同步，不能只更新其中一个
- 不要重复犯同样的错误；发现回归要记录到日志里

## 当前产品方向记忆
- 优先解决真实使用问题，不靠猜截图下结论
- 小窗口必须看得到真正项目数据，不能只看到表头或一小条
- 顶部区域不能长期占太多高度
- 进度反馈只允许两种动态：
  - 绿色进度填充长度变化
  - 百分比数字变化
- 不允许再出现整块闪烁、跑马灯、图标跳动或整张区域反复刷新
- 视觉方向已锁定为：
  - 自定义纯黑
  - 简约
  - 科技感
  - 高对比
  - 绿色作为唯一主要高亮
- 图标策略已锁定为：
  - 应用图标优先
  - 官方主图标统一兜底
- 长驻工作窗口要尽量独立：
  - 可单独移动
  - 可单独缩放
  - 可单独最小化
  - 尽量不互相锁死
- 用户重视：
  - 加载速度
  - 启动不卡
  - 多任务时不卡顿
  - 字体不裁切
  - 横向滚动一致
  - 删除更彻底
  - 迁移更稳
  - 记录完整、可交接

## 当前流程记忆
- 先记录，再执行
- 执行一段，记录一段
- 每次开工、阶段进展、准备中断、阶段完成，都要先写回 GitHub checkpoint
- 所有跨窗口接力先读 `AI_STATE.json`
- 所有关键节点都要写 checkpoint 到 `checkpoints/`
- 发布后记安装版时间戳
- 用户反馈后，先把“已确认事实 / 推测 / 未验证”分开

## 当前已知未结案重点
- 用户在 2026-04-24 最新安装版截图中再次确认：
  - 后台任务区仍有整块闪动体感
  - 小窗 / 大窗缩放时仍会卡顿、变形、短暂错位
  - 图标 fallback 仍旧模糊
  - 字体存在再次消失 / 裁切 / 发黑问题
  - 当前暗黑主题还不够高级，控件风格偏简陋
- 主窗口缩放时仍有卡顿、变形或短暂白屏体感
- 进度区域仍有整块闪动 / 跑马灯残留
- 主窗口与常驻工作窗口仍未完全统一成纯黑科技感
- 列表 fallback 图标质量仍不达标
- 启动速度与首屏体感仍需继续压缩到更接近“秒开感”
- 主窗口小窗布局仍未完全达到用户预期
- 顶部区占高问题仍需继续压缩
- 退出后进程偶尔残留的问题仍需单独收口
- GitHub 私有同步机制已经接通，但仍要坚持“先记录再推送”
