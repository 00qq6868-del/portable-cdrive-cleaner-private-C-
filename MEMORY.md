# 持久记忆

## 用户硬性要求
- 不要遗忘历史要求，必须把记忆写进项目文件，而不是只靠聊天上下文
- 同一个 AI 的不同窗口、不同 AI 的不同窗口，都要能接着做
- 所有“做到哪一步、没做到哪一步、下一步做什么”，必须可追溯
- 每次开始新的优化任务前，要先把计划和当前状态写下来
- 每完成一个部分，就立刻记录一个部分
- 已完成、未完成、待验证、回归失败，都必须明确记录
- 如果对话可能被截断，优先保存：
  - 当前进度
  - 当前计划
  - 修改过的文件
  - 还没修好的地方
  - 下一步该做什么
- 文件夹里的安装版和桌面入口要始终同步，不能只更新其中一个
- 不要重复犯同样的错误；发现回归要记录到日志里

## 当前产品方向记忆
- 优先解决真实使用问题，不靠猜截图下结论
- 小窗口必须看得到真正项目数据，不能只看到表头或一小条
- 顶部区域不能长期占太多高度
- 用户重视：
  - 加载速度
  - 启动不卡
  - 多任务时不卡顿
  - 字体不裁切
  - 横向滚动一致
  - 删除更彻底
  - 迁移更稳
  - 记录完整、可交接

## 当前流程记忆
- 先记录，再执行
- 执行一段，记录一段
- 发布后记安装版时间戳
- 用户反馈后，先把“已确认事实 / 推测 / 未验证”分开

## 当前已知未结案重点
- 主窗口小窗布局仍未完全达到用户预期
- 顶部区占高问题仍需继续压缩
- 退出后进程偶尔残留的问题仍需单独收口
- GitHub 私有同步机制尚未完全接通，只完成了本地文档骨架

